ip -4 -o addr show | awk '{print $4}' | cut -d/ -f1
#!/usr/bin/env bash
# Script to display all active IPv4 IPs on the machine

ifconfig | grep -oE 'inet (addr:)?([0-9]{1,3}\.){3}[0-9]{1,3}' | grep -v '127.0.0.1' | awk '{print $2}'
echo 127.0.0.1
